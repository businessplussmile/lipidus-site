{
  "entities": {
    "SiteImage": {
      "title": "Site Image",
      "description": "A single image stored in Firestore.",
      "type": "object",
      "properties": {
        "url": {
          "type": "string",
          "description": "Base64 data of the image."
        }
      },
      "required": ["url"]
    }
  },
  "firestore": {
    "/settings/img_collector": {
      "schema": "SiteImage",
      "description": "The collector image for the hero section."
    },
    "/settings/img_commercials": {
      "schema": "SiteImage",
      "description": "The commercials image for the recruitment section."
    },
    "/settings/img_partner": {
      "schema": "SiteImage",
      "description": "The partner image for the pro section."
    }
  }
}
